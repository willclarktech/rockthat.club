import { extendFragment } from 'meteor/vulcan:core';

extendFragment('PostsList', `
	soundcloud
  canBring
  minimumCharge
`);

extendFragment('PostsPage', `
	soundcloud
  canBring
  minimumCharge
`);
